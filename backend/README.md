# Satellite Tracker Backend

Python FastAPI backend for the satellite tracker project.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r backend/requirements.txt
```

## Run

```bash
uvicorn backend.app.main:app --reload
```
